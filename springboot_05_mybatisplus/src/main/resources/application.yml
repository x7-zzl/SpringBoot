#配置数据库信息
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/webframe?serverTimezone=UTC
    username: root
    password: yswysyydxj

#配置mybatis-plus相关的配置
#设置表名的前缀,id自增
mybatis-plus:
  global-config:
    db-config:
#      table-prefix: tbl_
      id-type: auto
server:
  port: 8080
